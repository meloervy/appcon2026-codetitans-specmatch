{
    "$schema": "https://www.schemastore.org/package.json",
    "name": "specmatch",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "echo 'Frontend assets are pre-compiled in public/build'",
        "dev": "vite",
        "dev:setup": "cp package.local.json package.json && npm install"
    }
}
